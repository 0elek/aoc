// import fs from "fs";

// let globalINDEX =  0 

// const input = fs.readFileSync("input.txt", "utf-8");
// const order = input.split("\n")[0].split(",");
// const boards = input.substring(order.join(",").length).split("\n\n");
// boards.shift();
// let tables = new Array();
// boards.forEach((board) => {
//   let rows = [];
//   board.split("\n").forEach((row) => {
//     rows.push(row.replaceAll("  ", " ").split(" "));
//   });
//   tables.push(rows);
// });
// const rules = {
//     didWin:(table)=> {
//         nows = order.splice(0, globalINDEX)
        

//     }
// };


// rules.didWin(tables[1])